{% extends "base.html.twig" %}

{% block body %}
<div class="form d-flex mb-2">
    {{ form_start(post_form) }}
        {{ form_rest(post_form) }}
        <button type="submit" class="btn btn-primary mb-5">Créer</button>
</div>
{% endblock %}